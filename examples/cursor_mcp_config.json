{
  "mcp": {
    "servers": {
      "enterprise-gateway": {
        "url": "http://localhost:8000",
        "description": "Enterprise MCP Gateway with multi-server broadcast",
        "enabled": true
      }
    }
  }
}
