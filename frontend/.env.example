# Policy Engine API URL
# Combined Mode (default): Both management and evaluation on same port
VITE_API_URL=http://localhost:9000

# Java Gateway URL (for direct MCP server configuration)
VITE_JAVA_GATEWAY_URL=http://localhost:8000

# Optional: Separate evaluation endpoint (only needed in split mode)
# VITE_EVAL_API_URL=http://localhost:9001
